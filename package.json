{
  "name": "scruburls",
  "version": "2.0.0",
  "description": "ScrubURLs: an unofficial Manifest V3 port of the ClearURLs browser extension",
  "private": true,
  "type": "module",
  "license": "LGPL-3.0-or-later",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "update-rules": "sh scripts/update-rules.sh",
    "package": "sh scripts/package.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
